%h2 電話番号の登録・確認
= form_for @telephone do |f|
  = render "devise/shared/error_messages", resource: @telephone
  .field
    = f.label :telephone
    %br/
    = f.text_field :telephone
  .actions
    = f.submit "Sign up"
= render "devise/shared/links"
